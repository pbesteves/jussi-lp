export type NavigationProps = {
  /**
   * A callback function that is passed to the component
   * to handle the toggle of the `<Navigation />` in
   * mobile devices.
   */
  onClick?: () => void;
};
